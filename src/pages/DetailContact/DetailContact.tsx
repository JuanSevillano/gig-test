
	import React from 'react';
	import classes from './DetailContact.module.scss'

	const DetailContact = (props: any) => {
	    return (
	    	<div className={classes.DetailContact}>DetailContact</div>
		)
	 }

	export default DetailContact
